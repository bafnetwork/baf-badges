
// TODO: write a component that shows NEAR address + optinal propic + dropdown for logout
// see https://github.com/bafnetwork/baf-badges/issues/9
export function AccountIndicator() {
	return (
		<></>
	)
}